// import React from 'react';

// const Buttons = props => {
//     return (
//         <div>
//            
//             {!props.isEditing ? (
//                 <button onClick={props.toggleEdit}>Update</button>
//             ) : (
//                     <button onClick={event => props.updateDayPost(event, props.id, props.reset())}> Submit </button>
//                 )}
        
//         </div>
//     );
// };
// export default Buttons;
